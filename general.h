#pragma once
#include <bits/stdc++.h>
#include <dirent.h>

using namespace std;
using pii = pair<int, int>;
using Matrix = vector<vector<int>>;
using Time = long long int;
using AdjList = vector<vector<pii>>;
const int INF = 1e9;

template <typename T>
T sq (T a){
    return (a) * (a);
}
